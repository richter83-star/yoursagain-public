# Public site environment variables
# Copy to .env.local for local development.

# Demo only — production uses a real auth provider.
NEXT_PUBLIC_DEMO_AUTH=true

# Client service API base URL (for future real backend).
CLIENT_SERVICE_API_URL=http://localhost:8001
